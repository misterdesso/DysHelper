/**
 * background.js
 * Currently minimal. Can later be used for toggling or syncing settings.
 */
chrome.runtime.onInstalled.addListener(() => {
  console.log("DysHelper Everywhere installed.");
});
